# Ash webui BUILD.gn
--- a/ash/webui/sample_system_web_app_ui/BUILD.gn
+++ b/ash/webui/sample_system_web_app_ui/BUILD.gn
@@ -1,13 +1,12 @@
-# Copyright 2019 The Chromium Authors
+# Copyright 2026 The Chromium Authors and Alex313031
 # Use of this source code is governed by a BSD-style license that can be
 # found in the LICENSE file.
 
+import("//build/config/chromeos/ui_mode.gni")
 import("//chrome/test/base/ash/js2gtest.gni")
 import("//third_party/closure_compiler/compile_js.gni")
 
-assert(is_chromeos, "Sample System Web App is ChromeOS only")
-assert(!is_official_build,
-       "Sample System Web App is only built for unofficial builds")
+assert(is_chromeos_ash, "Sample System Web App is ash-chrome only")
 
 static_library("sample_system_web_app_ui") {
   sources = [
# Ash webui mojom BUILD.gn
--- a/ash/webui/sample_system_web_app_ui/mojom/BUILD.gn
+++ b/ash/webui/sample_system_web_app_ui/mojom/BUILD.gn
@@ -1,12 +1,11 @@
-# Copyright 2021 The Chromium Authors
+# Copyright 2026 The Chromium Authors and Alex313031
 # Use of this source code is governed by a BSD-style license that can be
 # found in the LICENSE file.
 
+import("//build/config/chromeos/ui_mode.gni")
 import("//mojo/public/tools/bindings/mojom.gni")
 
-assert(is_chromeos, "Sample System Web App is ChromeOS only")
-assert(!is_official_build,
-       "Sample System Web App is only built for unofficial builds")
+assert(is_chromeos_ash, "Sample System Web App is ash-chrome only")
 
 mojom("mojo_bindings") {
   sources = [
# Ash webui resources trusted
--- a/ash/webui/sample_system_web_app_ui/resources/trusted/BUILD.gn
+++ b/ash/webui/sample_system_web_app_ui/resources/trusted/BUILD.gn
@@ -1,15 +1,14 @@
-# Copyright 2021 The Chromium Authors
+# Copyright 2026 The Chromium Authors and Alex313031
 # Use of this source code is governed by a BSD-style license that can be
 # found in the LICENSE file.
 
 import("//ash/webui/tools/mwc_path_mappings.gni")
+import("//build/config/chromeos/ui_mode.gni")
 import("//tools/typescript/ts_library.gni")
 import("//ui/webui/resources/tools/build_webui.gni")
 import("//ui/webui/resources/tools/generate_grd.gni")
 
-assert(is_chromeos, "Sample System Web App is ChromeOS only")
-assert(!is_official_build,
-       "Sample System Web App is only built for unofficial builds")
+assert(is_chromeos_ash, "Sample System Web App is ash-chrome only")
 
 build_webui("build") {
   grd_prefix = "ash_sample_system_web_app"
@@ -53,7 +52,6 @@
         [ _mwc_mapping[0] + "|" + rebase_path(_mwc_mapping[1], target_gen_dir) ]
   }
 
-  webui_context_type = "trusted"
   grit_output_dir = "$root_gen_dir/ash/webui"
   extra_grdp_deps = [ ":build_worker_grdp" ]
   extra_grdp_files = [ "$target_gen_dir/worker_resources.grdp" ]
# Ash webui resources untrusted
--- a/ash/webui/sample_system_web_app_ui/resources/untrusted/BUILD.gn
+++ b/ash/webui/sample_system_web_app_ui/resources/untrusted/BUILD.gn
@@ -1,12 +1,11 @@
-# Copyright 2021 The Chromium Authors
+# Copyright 2026 The Chromium Authors and Alex313031
 # Use of this source code is governed by a BSD-style license that can be
 # found in the LICENSE file.
 
+import("//build/config/chromeos/ui_mode.gni")
 import("//ui/webui/resources/tools/build_webui.gni")
 
-assert(is_chromeos, "Sample System Web App is ChromeOS only")
-assert(!is_official_build,
-       "Sample System Web App is only built for unofficial builds")
+assert(is_chromeos_ash, "Sample System Web App is ash-chrome only")
 
 build_webui("build") {
   grd_prefix = "ash_sample_system_web_app_untrusted"
@@ -28,6 +27,5 @@ build_webui("build") {
   ts_composite = true
   ts_deps = [ "//ui/webui/resources/mojo:build_ts" ]
 
-  webui_context_type = "untrusted"
   grit_output_dir = "$root_gen_dir/ash/webui"
 }
# Linux build deps (advancecomp, icoutils)
--- a/build/install-build-deps.py
+++ b/build/install-build-deps.py
@@ -210,6 +210,7 @@
 # Packages needed for development
 def dev_list():
   packages = [
+      "advancecomp",
       "autoconf",
       "binutils",
       "bison",
@@ -223,6 +224,7 @@
       "flex",
       "git-core",
       "gperf",
+      "icoutils",
       "libasound2-dev",
       "libatspi2.0-dev",
       "libbrlapi-dev",
@@ -264,15 +266,18 @@
       "lighttpd",
       "locales",
       "openbox",
+      "optipng",
       "p7zip",
       "patch",
       "perl",
       "pkgconf",
+      "pngcrush",
       "ripgrep",
       "rpm",
       "ruby",
       "uuid-dev",
       "wdiff",
+      "wget",
       "x11-utils",
       "xcompmgr",
       "xz-utils",
@@ -382,12 +382,14 @@ def lib_list():
       "libpango-1.0-0",
       "libpangocairo-1.0-0",
       "libpci3",
+      "libpcre3",
       "libpixman-1-0",
       "libspeechd2",
       "libstdc++6",
       "libsqlite3-0",
       "libuuid1",
       "libwayland-egl1",
+      "libwayland-egl1-mesa",
       "libx11-6",
       "libx11-xcb1",
       "libxau6",
@@ -491,6 +496,7 @@
       "zlib1g:i386",
       # 32-bit libraries needed e.g. to compile V8 snapshot for Android or armhf
       "linux-libc-dev:i386",
+      "linux-libc-dev-i386-cross",
       "libexpat1:i386",
       "libpci3:i386",
   ]
@@ -646,7 +652,9 @@
       "g++-arm-linux-gnueabihf",
       "gcc-arm-linux-gnueabihf",
       "libc6-dev-armhf-cross",
+      "libc6-dev-arm64-cross",
       "linux-libc-dev-armhf-cross",
+      "linux-libc-dev-arm64-cross",
   ]
 
   # Work around an Ubuntu dependency issue.
# Apple linker Rustc arg filter
--- a/build/toolchain/apple/linker_driver.py
+++ b/build/toolchain/apple/linker_driver.py
@@ -150,19 +150,23 @@
         self._linker_driver_pre_actions = {}
         self._compiler_driver_args = []
         for index, arg in enumerate(self._args[1:]):
+            BAD_RUSTC_ARGS = '-Wl,-plugin-opt=O[0-9],-plugin-opt=mcpu=.*'
             if arg.startswith(LINKER_DRIVER_COMPILER_ARG_PREFIX):
                 assert not self._driver_path
                 self._driver_path = arg[len(LINKER_DRIVER_COMPILER_ARG_PREFIX
                                             ):]
+                if not re.match(BAD_RUSTC_ARGS, arg):
+                    self._compiler_driver_args.append(arg)
             elif arg.startswith(LINKER_DRIVER_ARG_PREFIX):
                 # Convert driver actions into a map of name => lambda to invoke.
                 self._process_driver_arg(arg)
+                if not re.match(BAD_RUSTC_ARGS, arg):
+                    self._compiler_driver_args.append(arg)
             else:
                 # TODO(crbug.com/40268754): On Apple, the linker command line
                 # produced by rustc for LTO includes these arguments, but the
                 # Apple linker doesn't accept them.
                 # Upstream bug: https://github.com/rust-lang/rust/issues/60059
-                BAD_RUSTC_ARGS = '-Wl,-plugin-opt=O[0-9],-plugin-opt=mcpu=.*'
                 if not re.match(BAD_RUSTC_ARGS, arg):
                     self._compiler_driver_args.append(arg)
 
# VS toolchain hash update
# --- a/build/vs_toolchain.py
# +++ b/build/vs_toolchain.py
# @@ -59,7 +61,7 @@
#  # * docs/windows_build_instructions.md
#  #   Make sure any version numbers in the documentation match the code.
#  #
# -TOOLCHAIN_HASH = '68a20d6dee'
# +TOOLCHAIN_HASH = 'b3d793284e'
#  SDK_VERSION = '10.0.26100.0'
#  
#  # Visual Studio versions are listed in descending order of priority.
# Thorium flag entries in browser build
--- a/chrome/browser/BUILD.gn
+++ b/chrome/browser/BUILD.gn
@@ -1601,6 +1601,8 @@
     "tab_group_sync/tab_group_sync_utils.h",
     "tab_group_sync/tab_group_trial.cc",
     "tab_group_sync/tab_group_trial.h",
+    "thorium_flag_choices.h",
+    "thorium_flag_entries.h",
     "tpcd/experiment/eligibility_service.cc",
     "tpcd/experiment/eligibility_service.h",
     "tpcd/experiment/eligibility_service_factory.cc",
# Linux installer compiler_opt config
--- a/chrome/installer/linux/BUILD.gn
+++ b/chrome/installer/linux/BUILD.gn
@@ -4,6 +4,7 @@
 
 import("//build/config/c++/c++.gni")
 import("//build/config/chrome_build.gni")
+import("//build/config/compiler_opt.gni")
 import("//build/config/sanitizers/sanitizers.gni")
 import("//build/config/sysroot.gni")
 import("//build/linux/strip_binary.gni")
@@ -22,6 +22,10 @@ if (bundle_widevine_cdm) {
   import("//media/cdm/library_cdm/cdm_paths.gni")
 }
 
+if (enable_library_cdms && !bundle_widevine_cdm) {
+  import("//media/cdm/library_cdm/cdm_paths.gni")
+}
+
 declare_args() {
   # Enables checks that ensure the packages are installable on all supported
   # distributions.
@@ -24,13 +25,34 @@
 declare_args() {
   # Enables checks that ensure the packages are installable on all supported
   # distributions.
-  enable_distro_version_check = true
+  enable_distro_version_check = false
 }
 
 assert(is_linux)
 
+# Determine SIMD instruction set suffix for package naming,
+# mirroring the logic in chrome/installer/mini_installer/BUILD.gn.
+_simd_suffix = ""
+if (current_cpu == "x86" || current_cpu == "x64") {
+  if (use_sse2 && !use_sse3) {
+    _simd_suffix = "_SSE2"
+  } else if (use_sse3 && !use_sse41 && !use_sse42) {
+    _simd_suffix = "_SSE3"
+  } else if (use_sse41 && !use_sse42) {
+    _simd_suffix = "_SSE4"
+  } else if (use_sse42 && !use_avx) {
+    _simd_suffix = "_SSE4.2"
+  } else if (use_avx && !use_avx2) {
+    _simd_suffix = "_AVX"
+  } else if (use_avx2 && !use_avx512) {
+    _simd_suffix = "_AVX2"
+  } else if (use_avx512) {
+    _simd_suffix = "_AVX512"
+  }
+}
+
 packaging_files_executables = [
-  "$root_out_dir/chrome",
+  "$root_out_dir/thorium",
   "$root_out_dir/chrome_crashpad_handler",
   "$root_out_dir/chrome_management_service",
   "$root_out_dir/chrome_sandbox",
@@ -91,6 +95,11 @@ packaging_files_binaries = packaging_files_executables + packaging_files_shlibs
 # (e.g. all locales, resources, etc.)
 packaging_files = packaging_files_binaries + [
                     "common/installer.py",
+                    "$root_out_dir/initial_preferences",
+                    "$root_out_dir/resources/inspector_overlay/inspector_overlay_resources.grd",
+                    "$root_out_dir/resources/inspector_overlay/main.js",
+                    "$root_out_dir/thorium.svg",
+                    "$root_out_dir/ui_resources_100_percent.pak",
                     "$root_out_dir/xdg-mime",
                     "$root_out_dir/xdg-settings",
                     "$root_out_dir/locales/en-US.pak",
@@ -170,7 +192,7 @@
 }
 
 strip_binary("strip_chrome_binary") {
-  binary_input = "$root_out_dir/chrome"
+  binary_input = "$root_out_dir/thorium"
   deps = [ "//chrome" ]
 }
 
@@ -211,6 +220,35 @@ strip_binary("strip_chrome_sandbox") {
   deps = [ "//sandbox/linux:chrome_sandbox" ]
 }
 
+strip_binary("strip_thorium_shell") {
+  testonly = true
+  binary_input = "$root_out_dir/thorium_shell"
+  deps = [ "//content/shell:thorium_shell" ]
+}
+
+if (enable_library_cdms) {
+  strip_binary("strip_clearkey_cdm") {
+    testonly = true
+    binary_input = "$root_out_dir/$clearkey_cdm_path/libclearkeycdm.so"
+    stripped_binary_output =
+        "$root_out_dir/$clearkey_cdm_path/libclearkeycdm.so.stripped"
+    deps = [ "//media/cdm/library_cdm/clear_key_cdm:clear_key_cdm_bin" ]
+  }
+}
+
+group("thorium_package_extras") {
+  testonly = true
+  deps = [
+    # ":strip_thorium_shell",
+    "//chrome/test/chromedriver:chromedriver_server",
+    "//content/shell:copy_shell_resources",
+    "//content/shell:pak",
+  ]
+  if (enable_library_cdms) {
+    deps += [ ":strip_clearkey_cdm" ]
+  }
+}
+
 strip_binary("strip_libEGL_shlib") {
   binary_input = "$root_out_dir/libEGL.so"
   deps = [ "//third_party/angle:libEGL" ]
@@ -237,10 +259,7 @@
 # is_component_build=false before building this target.
 group("linux") {
   deps = [
-    ":beta",
-    ":canary",
     ":stable",
-    ":unstable",
   ]
 }
 
@@ -301,6 +339,7 @@ copy("deb_packaging_files") {
   sources = [
     "debian/changelog.template",
     "debian/control.template",
+    # "debian/manual_recommends",
     "debian/postinst",
     "debian/postrm",
     "debian/prerm",
@@ -308,6 +347,16 @@ copy("deb_packaging_files") {
   outputs = [ "$root_out_dir/installer/debian/{{source_file_part}}" ]
 }
 
+generated_file("thorium_initial_preferences") {
+  outputs = [ "$root_out_dir/initial_preferences" ]
+  contents = "{\n}\n"
+}
+
+copy("thorium_svg") {
+  sources = [ "//chrome/app/theme/chromium/product_logo.svg" ]
+  outputs = [ "$root_out_dir/thorium.svg" ]
+}
+
 copy("theme_files") {
   visibility = [ ":*" ]
   sources = [
@@ -332,7 +351,7 @@
 if (!is_chromeos) {
   copy("rpm_packaging_files") {
     visibility = [ ":*" ]
-    sources = [ "rpm/chrome.spec.template" ]
+    sources = [ "rpm/thorium.spec.template" ]
     outputs = [ "$root_out_dir/installer/rpm/{{source_file_part}}" ]
   }
 }
@@ -383,6 +432,8 @@ group("installer_deps") {
     ":strip_chrome_management_service",
     ":strip_chrome_sandbox",
     ":theme_files",
+    ":thorium_initial_preferences",
+    ":thorium_svg",
     "//chrome",
     "//chrome:packed_resources",
     "//chrome/browser/enterprise/connectors/device_trust/key_management/installer/management_service:chrome_management_service",
@@ -390,6 +441,8 @@ group("installer_deps") {
     "//components/crash/core/app:chrome_crashpad_handler",
     "//components/privacy_sandbox/privacy_sandbox_attestations/preload:component",
     "//sandbox/linux:chrome_sandbox",
+    "//third_party/devtools-frontend/src/inspector_overlay:build_inspector_overlay",
+    "//ui/resources:copy_ui_resources_100_percent",
   ]
   if (bundle_widevine_cdm) {
     public_deps += [ "//third_party/widevine/cdm" ]
@@ -424,13 +443,11 @@
 # channel:
 #   Name of the channel.
 template("linux_package") {
-  assert(defined(invoker.channel))
-  channel = invoker.channel
 
   if (is_chrome_branded) {
     package = "google-chrome"
   } else {
-    package = "chromium-browser"
+    package = "thorium-browser"
   }
 
   deb_target_name = "${target_name}_deb"
@@ -458,8 +475,16 @@
       assert(false, "Linux installer not configured for this architecture.")
     }
 
+    # For x64, the SIMD suffix alone identifies the build variant (drop arch).
+    # For all other architectures, keep the arch identifier.
+    if (current_cpu == "x64") {
+      _deb_name_suffix = _simd_suffix
+    } else {
+      _deb_name_suffix = "_${deb_arch}${_simd_suffix}"
+    }
+
     inputs = packaging_files
-    outputs = [ "$root_out_dir/${package}-${channel}_${chrome_version_full}-1_${deb_arch}.deb" ]
+    outputs = [ "$root_out_dir/${package}_${chrome_version_full}${_deb_name_suffix}.deb" ]
 
     args = [
       "-a",
@@ -470,6 +495,8 @@
       invoker.channel,
       "-d",
       branding_path_component,
+      "-n",
+      _deb_name_suffix,
       "-o",
       rebase_path(root_out_dir, root_build_dir),
       "-s",
@@ -499,7 +526,7 @@
       } else if (current_cpu == "arm") {
         rpm_arch = "armhf"
       } else if (current_cpu == "arm64") {
-        rpm_arch = "aarch64"
+        rpm_arch = "arm64"
       } else if (current_cpu == "mipsel") {
         rpm_arch = "mipsel"
       } else if (current_cpu == "mips64el") {
@@ -512,8 +539,16 @@
         assert(false, "Linux installer not configured for this architecture.")
       }
 
+      # For x64, the SIMD suffix alone identifies the build variant (drop arch).
+      # For all other architectures, keep the arch identifier.
+      if (current_cpu == "x64") {
+        _rpm_name_suffix = _simd_suffix
+      } else {
+        _rpm_name_suffix = "_${rpm_arch}${_simd_suffix}"
+      }
+
       inputs = packaging_files
-      outputs = [ "$root_out_dir/${package}-${channel}-${chrome_version_full}-1.${rpm_arch}.rpm" ]
+      outputs = [ "$root_out_dir/${package}_${chrome_version_full}${_rpm_name_suffix}.rpm" ]
 
       args = [
         "-a",
@@ -524,6 +559,8 @@
         invoker.channel,
         "-d",
         branding_path_component,
+        "-n",
+        _rpm_name_suffix,
         "-o",
         rebase_path(root_out_dir, root_build_dir),
         "-t",
@@ -551,12 +588,3 @@
 linux_package("stable") {
   channel = "stable"
 }
-linux_package("beta") {
-  channel = "beta"
-}
-linux_package("unstable") {
-  channel = "unstable"
-}
-linux_package("canary") {
-  channel = "canary"
-}
--- a/components/vector_icons/BUILD.gn
+++ b/components/vector_icons/BUILD.gn
@@ -1,8 +1,9 @@
-# Copyright 2017 The Chromium Authors
+# Copyright 2026 The Chromium Authors and Alex313031
 # Use of this source code is governed by a BSD-style license that can be
 # found in the LICENSE file.
 
 import("//components/vector_icons/vector_icons.gni")
+import("//third_party/blink/public/public_features.gni")
 
 _icons_gen_dir = "$root_build_dir/gen/components/vector_icons"
 
@@ -39,6 +40,7 @@
       "bluetooth_off_chrome_refresh.icon",
       "bluetooth_scanning.icon",
       "bluetooth_scanning_chrome_refresh.icon",
+      "bookmark_folder.icon",
       "business.icon",
       "business_chrome_refresh.icon",
       "call.icon",
@@ -105,6 +107,7 @@
       "flight.icon",
       "folder.icon",
       "folder_chrome_refresh.icon",
+      "folder_managed.icon",
       "folder_managed_refresh.icon",
       "folder_open.icon",
       "font_download.icon",
@@ -131,6 +134,7 @@
       "image_search.icon",
       "info_outline.icon",
       "info_refresh.icon",
+      "info_regular.icon",
       "insert_drive_file_outline.icon",
       "install_desktop.icon",
       "install_desktop_off.icon",
@@ -204,6 +208,7 @@
       "receipt_long.icon",
       "reload.icon",
       "reload_chrome_refresh.icon",
+      "restore.icon",
       "replay.icon",
       "replay_10.icon",
       "router.icon",
@@ -289,6 +294,14 @@
       "work.icon",
     ]
 
+    if (is_thorium_build) {
+      sources += [
+        "thorium/reload_thorium.icon",
+        "thorium/reload_chrome_refresh_thorium.icon",
+        "thorium/restore_tab.icon",
+      ]
+    }
+
     if (is_chromeos) {
       sources += [
         "notification_download.icon",
--- a/sandbox/linux/BUILD.gn
+++ b/sandbox/linux/BUILD.gn
@@ -295,6 +295,9 @@
       "suid/process_util_linux.c",
       "suid/sandbox.c",
     ]
+
+    cflags = [ "-fPIE", ]
+    ldflags = [ "-pie", ]
   }
 }
 
# Widevine CDM build config
--- a/third_party/widevine/cdm/BUILD.gn
+++ b/third_party/widevine/cdm/BUILD.gn
@@ -40,8 +40,13 @@
   ]
 
   if (enable_widevine_cdm_host_verification) {
-    assert(is_win || is_mac)
+    assert(target_os == "win" || is_mac)
     widevine_cdm_binary_files += [ "${widevine_cdm_root}/${cdm_file_name}.sig" ]
+  }
+
+  if (target_os == "win") {
+    assert(target_os == "win")
+    widevine_cdm_binary_files += [ "${widevine_cdm_root}/${cdm_file_name}.lib" ]
   }
 }
 
# Widevine platform enablement
--- a/third_party/widevine/cdm/widevine.gni
+++ b/third_party/widevine/cdm/widevine.gni
@@ -12,13 +12,12 @@
   # Google Chrome for Testing and Android. For Fuchsia, see crbug.com/1354347.
   # Can be optionally enabled in Chromium on non-Android platforms. Please see
   # //src/third_party/widevine/LICENSE file for details.
-  enable_widevine = ((is_chrome_branded || is_chrome_for_testing_branded) &&
-                     !is_fuchsia) || is_android
+  enable_widevine = !is_fuchsia
 }
 
 # Disable Widevine when running linux-chromeos.
 if (is_chromeos && !is_chromeos_device) {
-  enable_widevine = false
+  enable_widevine = true
 }
 
 # Widevine CDM is available as a library CDM on the following platforms and
@@ -27,7 +26,7 @@
 library_widevine_cdm_available =
     (is_chromeos &&
      (target_cpu == "x64" || target_cpu == "arm" || target_cpu == "arm64")) ||
-    (target_os == "linux" && target_cpu == "x64") ||
+    (target_os == "linux") ||
     (target_os == "mac" && (target_cpu == "x64" || target_cpu == "arm64")) ||
     (target_os == "win" &&
      (target_cpu == "x86" || target_cpu == "x64" || target_cpu == "arm64"))
@@ -64,19 +63,19 @@
   widevine_root = "."
 }
 
-# Enable Widevine CDM host verification, which will sign additional binaries
-# with Widevine certs and copy signature files as needed.
-enable_widevine_cdm_host_verification =
-    enable_library_widevine_cdm && enable_cdm_host_verification
+declare_args() {
+  # Enable Widevine CDM host verification, which will sign additional binaries
+  # with Widevine certs and copy signature files as needed.
+  enable_widevine_cdm_host_verification =
+      enable_library_widevine_cdm && enable_cdm_host_verification
 
-declare_args() {
   # If set, and Widevine CDM host verification signing failed due to no signing
   # cert, the failure will be ignored. Otherwise the build process will fail.
   # Set to false by default for official build to catch missing cert error.
   # For developers building with "is_official_build" locally without Widevine
   # signing certs, please manually set `ignore_missing_widevine_signing_cert`
   # to true to suppress the error.
-  ignore_missing_widevine_signing_cert = !is_official_build
+  ignore_missing_widevine_signing_cert = true
 }
 
 template("widevine_sign_file") {
# V8 snapshot rpath config
--- a/tools/v8_context_snapshot/BUILD.gn
+++ b/tools/v8_context_snapshot/BUILD.gn
@@ -101,6 +101,10 @@
         "//v8",
       ]
 
+      if (is_linux || is_chromeos) {
+        configs += [ "//build/config/gcc:rpath_for_built_shared_libraries" ]
+      }
+
       configs += [
         "//v8:external_startup_data",
         ":disable_icf",
# Widevine version update
--- a/third_party/widevine/README.chromium
+++ b/third_party/widevine/README.chromium
@@ -1,16 +1,18 @@
 Name: Widevine Content Decryption Module
 Short Name: Widevine CDM
 URL: https://www.widevine.com
-Version: N/A
+Version: 4.10.3050.0
 Revision: c96f53fde16f37b1a6cc2b6c468af3c4a364f426
 Update Mechanism: Manual
-License: Commercial
+License: By exception only
 License File: LICENSE
 Security Critical: yes
-Shipped: yes
 License Android Compatible: no
 
 Description:
 This directory contains build files for integrating Widevine CDM into Chrome.
 No third-party files are checked in here. If you are interested in integrating
 Widevine CDM into your product, please visit the URL above.
+
+Local Modifications:
+Prebuilt binaries for bundle_widevine_cdm = true

